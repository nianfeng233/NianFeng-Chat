# 风语 v0.41.1

**发布日期**：2026-09  
**许可证**：Apache License 2.0

本次为文档与发布内容修正版本，不包含功能改动。

## 修正

- **README 重写并分为中英双语**：
  - 中文：[`README.md`](../README.md)
  - English: [`README.en.md`](../README.en.md)
  - 两个文件顶部均注明：README 由 DeepSeek（AI）协助整理生成，实际功能以代码和测试为准。
  - 删除原有宣传性表述与不属于本项目的理念引用，只保留对项目结构、功能、对话机制与使用方式的
    事实性说明。
- **中文文件名清理**：根目录 `文档.txt` 改名为 [`docs/LEGACY-DESIGN.md`](../docs/LEGACY-DESIGN.md)，
  并更新 `docs/ARCHITECTURE.md`、`docs/CHAT-FLOW.md`、`docs/STATUS.md` 中的引用。
- **项目描述中性化**：`package.json` 的 description 与设置界面中的标语改为对项目的事实描述，
  不再使用其他项目的理念口号。
- **协议澄清**：
  - `main` 分支与 v0.41.0 及之后版本使用 **Apache License 2.0**（[`LICENSE`](../LICENSE)、[`NOTICE`](../NOTICE)）。
  - v0.40.0 是误发布的历史版本，使用创建当时的旧许可证；为避免协议混淆，该版本的 GitHub Release 与 tag 均已撤回。
- **发布内容复核**：发布仓库由 `release/web/source` 同步生成，不包含 workspace 中的用户数据、
  配置、密钥、日志或本地 demo 文件；发布前强制通过敏感信息扫描。

## 资产

| 文件 | 说明 |
|---|---|
| `fengyu-desktop-v0.41.1.exe` | Windows 桌面单文件 |
| `fengyu-web-deploy-v0.41.1.zip` | Web 可部署版（自带便携 Node） |
| `fengyu-web-source-v0.41.1.zip` | Web 纯净源码 |

## 验证

- `npm test` 通过；`scripts/smoke.mjs` 207 项通过。
- `node scripts/prepare-publish.mjs` 安全扫描通过。
- 仓库中不包含 `user_data`、`config.json`、`sessions.json`、`.secret-key`、日志或本地 demo 文件。
